[project]
name = "flint-bench"
version = "0.3.0"
description = "Flint / GB10 local LLM long-context benchmark harness with scoring and memory sampling"
requires-python = ">=3.11"
dependencies = [
  "pyyaml>=6.0.2",
  "pandas>=2.2.0",
]

[tool.uv]
package = false
